<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @city.name %>
</p>

<% @albums.each do |album| %>

	<tr>
        <td><%= album.title %></td>
        <td><%= link_to "Show", city_album_path(@city,album) %></td>
        <td><%= link_to 'Destroy', city_album_path(@city,album), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
	
<% end %>

<%= link_to "new album", new_city_album_path(@city) %>
<%= link_to 'Back', root_path %>
